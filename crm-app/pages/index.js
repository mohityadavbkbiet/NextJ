// Main index page
import Layout from '../components/Layout';

const Home = () => {
    return (
        <Layout>
            <h1>Welcome to the CRM App</h1>
            <p>This is a simple Customer Relationship Management app.</p>
        </Layout>
    );
};

export default Home;
